FROM node:18-alpine

WORKDIR /app

# Copy ROOT package.json (server deps)
COPY package*.json ./

RUN npm install --omit=dev --no-audit --no-fund

# Copy server source code
COPY server/ ./server

EXPOSE 3000

CMD ["node", "server/index.js"]